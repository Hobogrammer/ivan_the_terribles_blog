<div class="comment">
  <h3>Comment <%= comment.id %>:</h3>
  <p><%= comment.body %></p>
  <div class="reply" id=<%= dom_id(comment) %>>
    <%= link_to "View Replies", reply_partial_path(comment), { :remote => true} %>
  </div>
</div>
